---
topic: wdi-autopilot run log — mandate DEC-018
artifact: .control/decisions/DEC-018-autopilot-mandate-r3-close-every-open-fr.md
updated: 2026-09-04T00:00
---

Mandate: `DEC-018`. Parameters at `.control/registry/decisions.yaml` → `DEC-018.mandate`
(`from_gate: G5`, `scope: all`, `parked: []`, `smoke_test: owner`, `loop: 10m`,
`expires: 2026-09-11`). Start commit: `f9c0957e549bd1da36c536b9a23d6b873c70288e`, branch
`autopilot/r3`.

| When | Where | Decided | Instead of | Cost if wrong | Landed in |
|---|---|---|---|---|---|
| Preflight, commit f9c0957 | wdi-autopilot Door 1 | Mandate accepted with `from_gate: G5`, `scope: all`, `parked: []`, `smoke_test: owner`, `loop: 10m`, `expires: 2026-09-11` | — the owner answered the preflight page directly | — | `DEC-018`, `decisions.yaml` |
| Iter 1, commit bc9cfad | wdi-autopilot Door 2, routing table row "A spec is open" | `wdi-build` MUST NOT be run in this repo (`AGENTS.md` § Skills that MUST NOT be run) — substituted the direct engine flow it names instead: `.scratch/bundle-library` already has `spec.md` and tickets 01-19 cut via `/to-spec`/`/to-tickets`, so this iteration skipped straight to `/implement-spec` on the frontier (tickets 10, 11) rather than opening it as a `SPEC-N` row in `specs.yaml` | `wdi-build` Phase 1/2/3 verbatim, which AGENTS.md forbids | One setting changes: if this reading of AGENTS.md is wrong, the spec needs a `SPEC-N` row and a formal `wdi-build` open before its tickets count as landed | dispatched builder agent `a9a672926847f16fc`, worktree `D:\Developer\wiradigital.id\snapdown` branch `feat/bundle-library` |
| Iter 1, commit bc9cfad | wdi-autopilot Door 2 | Dispatched the whole `bundle-library` spec (tickets 10-18) to one builder agent in its existing worktree/branch, rather than one dispatch per ticket, since `/implement-spec` already owns frontier sequencing across the ticket DAG | Coordinating each ticket's dispatch from here | If the agent stalls or drifts scope, the next iteration reads its report and corrects rather than having caught it mid-flight | agent `a9a672926847f16fc`, no ledger file changed yet — pending its report |
| Iter 1, agent report | wdi-autopilot Door 2, routing table row "A spec just closed" | Builder agent `a9a672926847f16fc` reported: `bundle-library` (tickets 10-19) was already built, reviewed and merged as PR #38 on 2026-09-03, by a session before this mandate existed — every ticket's `**Status:**` line just never got updated. Nothing to build; treated as "spec just closed" and reconciled instead | Re-implementing tickets already shipped, which the agent correctly refused to do | If this reading is wrong (some hidden regression PR #38 didn't cover), the next smoke-test pass at close is where it surfaces, since `smoke_test: owner` exercises every closed FR's proof of done | — |
| Iter 1, commit 19589bb | wdi-autopilot Door 2, "A spec just closed" — scoped reconcile, `bundle`/`finding` only, not a full-corpus pass | Corrected 9 ticket status lines + `spec.md` from `ready-for-agent` to `done`, with merge SHAs. Corrected `FR-41`'s statement/proof to state the shared-Bundle-sealing consequence the built confirmation dialog already shows (ticket 17). Added the `bundle.updated_at` last-edited amendment to `SRS-bundle.md` and `SDD-bundle.md` (migration 9, exact field name read from the built code, not guessed). Corrected `inventory-screen.md` rows 8/10/16 to name the real Slint components (`SdLibrary`, `SdReviewUpdate`, `SdReclaimSpace`) instead of a dead React-route plan or "PLAN ONLY". Renamed `LC-014` to `library` and registered `LC-033 review-update`, `LC-034 reclaim-space` in `components.yaml` | A full `wdi-reconcile` pass over the whole corpus, which this iteration narrowed to the `bundle`/`finding` surfaces this spec actually touched | If the narrower scope missed drift elsewhere, a later full reconcile pass catches it — nothing here was a one-way door | `.scratch/bundle-library/*`, `.control/registry/requirements-capture-to-markdown.yaml`, `.what/bundle/SRS-bundle.md`, `.how/bundle/SDD-bundle.md`, `.how/_platform/inventory-screen.md`, `.control/registry/components.yaml`, `.control/generated/*` (regenerated), `.what-rendered/*`, `.how-rendered/*` (regenerated) |
| Iter 1, commit 19589bb | wdi-autopilot Door 2 | Left `inventory-screen.md` rows 0, 2, 4-7, 9, 11-15 untouched — they are the same 2026-08-23 React-era snapshot the document already flags as a debt "still owed", and this iteration only had evidence for the three rows the closed spec actually rebuilt | Re-planning the whole table against Slint now, while touching components (`finding`, `settings`, `sharing`, `agent-access`) this spec never touched | If left too long the debt compounds; named explicitly here so the next full reconcile pass (or the owner) can pick it up rather than re-discovering it | — (no edit; logged as a follow-up) |
| Iter 1, commit 19589bb | wdi-autopilot Door 2 | Left `.scratch/bundle-library/` on disk rather than distilling/deleting it now that the spec is closed | Treating "distillation" (removing the spec folder once its durable truth has left it) as part of this bookkeeping pass | If left indefinitely it is dead weight, not a risk; a deliberate distillation pass is a better home for that judgement than a status-line fix | — (no edit; logged as a follow-up) |
| Iter 2, commit 8b8546d | wdi-autopilot Door 2, routing table row "G4 clear for a candidate row" (before picking one) | Checked `W8` ("Capture becomes real", `status: open`, `fr: [FR-1,2,4,8]`) against git history before trusting the registry, on the strength of iteration 1's finding — same shape found again: all six stories (`W8-S1`..`S6`) carry merge commits, the last (`1e84c86`) explicitly closing `FR-8`. Closed `W8` in `specs.yaml` | Trusting `status: open` and treating `FR-1/2/4/8` as still-open scope | If wrong, `smoke_test: owner` catches it at close since those FRs' proof-of-done gets exercised then | `.control/registry/specs.yaml` |
| Iter 2, commit 8b8546d | wdi-autopilot Door 2 | Found 11 commits tagged `(W9)` in git history with no row anywhere in `specs.yaml`, and a preliminary grep hitting "blur", "arrow", "callout", "zoom", "paste", "context-menu" and "z-order" across `apps/desktop/ui` and `apps/desktop/src` — exactly the FR-30..38 backlog `wdi-report`'s candidate table lists as unscheduled. Dispatched a **verification-only** agent (no code changes) to check each of FR-30..38 against the real code and its tests before scheduling any of it as new work | Running `wdi-report intent estimate` and picking a candidate from FR-30..43 to build immediately, which risked dispatching a builder onto work already shipped a second time in two iterations | If the verification is wrong, the next iteration's builder dispatch would catch the mismatch when tests it expected to write already exist — cost is a wasted dispatch, not a bad merge | agent `a309ef3a13e6a54a5`, no files changed yet — pending its report |
| Iter 3 | wdi-autopilot Door 2 | No decision — the verification agent was still running; starting new work before its report would have risked the exact duplicate-dispatch mistake it exists to prevent | Waiting one more firing | — | — |
| Iter 4, agent report | wdi-autopilot Door 2 | Verification agent `a309ef3a13e6a54a5` confirmed: FR-30, 31, 32, 33, 36, 37, 38 are all **already built and tested** (the 11 `(W9)` commits were a red herring — pre-`DEC-007` archived Tauri work, irrelevant to the current Slint tree). Genuine remaining backlog from this batch is narrower than feared: only `FR-34` (canvas zoom) and `FR-35` (clipboard paste) are unbuilt. It also found a live regression (`cargo test --workspace` red on `test_library_wiring.rs`, `BUG-103`) and a corpus contradiction (`SRS-finding.md`'s Non-Goals denying annotations the same file's own `satisfies:` and `UC-27` already promise) | Trusting the registry's "never scheduled" reading and building all nine FRs fresh | Confirmed by reading the actual test files and burn-level pixel tests the agent cited, not taken on the agent's word alone | — |
| Iter 4, commit 998a693 | wdi-autopilot Door 2, "Code right, document wrong" | Corrected `SRS-finding.md`'s Non-Goals bullet (annotations are built; only freehand drawing is still out of scope) and `FR-33`'s proof line ("Delete or Backspace" → "Delete", matching the owner's own recorded decision that Backspace must not delete an element). Reopened `BUG-103` — `status: fixed` no longer holds; not yet diagnosed | Leaving the stale Non-Goal and the false `status: fixed` standing | If wrong, the next reconcile pass or the owner's review at close catches it | `.what/finding/SRS-finding.md`, `.control/registry/requirements-capture-to-markdown.yaml`, `.control/registry/defects.yaml`, `.control/generated/*`, `.what-rendered/*`, `.how-rendered/*` |
| Iter 4, commit 998a693 | wdi-autopilot Door 2 | Dispatched a builder to run `wdi-systematic-debugging` on `BUG-103`'s regression before proposing any fix, per `AGENTS.md`'s own rule, rather than patching the Slint property back in blind | Guessing the cause (a property-ordering change in a later ticket's edit to `library.slint`) and patching without knowing why it moved | If the guess were wrong the same regression could resurface after a plausible-looking fix; a diagnosed fix is the one this repo's own rules ask for | agent `a54de1d4adffa3308`, no files changed yet — pending its report |
| Iter 4 | wdi-autopilot Door 2 | Left `FR-34` (zoom) and `FR-35` (paste) — the two FRs the verification confirmed as genuine, unbuilt backlog — for the next iteration rather than scheduling them in the same pass as the `BUG-103` fix dispatch | Scheduling new-feature work and a regression fix in the same iteration | A live regression outranks new feature work; keeping them separate keeps each dispatch's scope legible | — (no edit; logged as what the next firing does) |
| Iter 5-6 | wdi-autopilot Door 2 | No decision — `BUG-103`'s fix agent was still running both firings | Starting FR-34/35 scheduling before the regression fix landed | — | — |
| Iter 7, commits ece4abc/52d17b0 | wdi-autopilot Door 2 | `BUG-103`'s second occurrence diagnosed via `wdi-systematic-debugging`: not a code defect at all — `library.slint`'s `danger: true` wiring was correct the whole time; a Windows checkout with `core.autocrlf=true` and no `.gitattributes` silently rewrote every `*.slint` file's line endings to CRLF on disk, and the one test reading its file raw (`fs::read_to_string`, no `flat()` normalization, unlike every sibling assertion in the same file) broke on the mismatch. Fixed by forcing `*.slint text eol=lf` in a new `.gitattributes`, re-checked-out, `status: fixed` restored with the root cause appended (original entry kept). Re-ran `cargo test --workspace` myself: all pass, including the named test | Patching the Slint property back in blind, which would have "fixed" nothing since it was never broken | If wrong, the same test would still be red — checked directly rather than trusted on the agent's report alone | `.gitattributes`, `.control/registry/defects.yaml` (agent's commits `ece4abc`, `52d17b0`) |
| Iter 7, routing table row "G4 clear for a candidate row" | wdi-autopilot Door 2 | With `BUG-103` confirmed fixed, dispatched `FR-34` (canvas zoom) and `FR-35` (clipboard paste) as new work — the two FRs the FR-30..38 verification confirmed as genuinely unbuilt. Bundled both into one dispatch/spec (component `finding`, `mode: guarded`, no shared code path between them but both thin) rather than opening two separate agent dispatches, and instructed the builder to isolate into its own new worktree/branch (`feat/canvas-zoom-clipboard-paste`, off `main`) — neither this coordinator worktree nor the sibling `feat/bundle-library` one is right for this | Two separate dispatches, or building in a shared worktree | If bundling turns out wrong (the two features fight over review scope), the builder itself can still split them into two tickets — which the brief explicitly left to its judgment | agent `a97763b748816b269`, no files changed yet — pending its report |
| Iter 13, PR #39 | wdi-autopilot Door 2, "A spec just closed" | `FR-34`/`FR-35` builder reported both tickets closed, a separately-dispatched `/code-review` pass with zero blocking findings on both axes, and no corpus conflicts. Independently re-verified rather than trusted outright: read the actual commits (`49e9562`, `396550c`, `d97b82e`) and branch in the worktree the agent created, re-ran `cargo test --workspace --no-fail-fast` myself — green. No document correction needed this time; the agent's own report already cited every `FR`/`UC` by id and found nothing to fix | Trusting the agent's report without an independent check, the same gap that let `BUG-103`'s CRLF cause go unnoticed if not re-run | If my re-run had disagreed, this row would say so; it didn't | `feat/canvas-zoom-clipboard-paste` branch, PR #39 (not merged by this run) |
| Iter 13 | wdi-autopilot Door 2 | With `FR-34`/`FR-35` closed, every FR in `requirements-capture-to-markdown.yaml` (`FR-1`..`FR-43`) now traces to a closed wave or a closed spec — but before reading that as "every FR in scope closed" (which would move to § Finish), checked `requirements-agent-handoff.yaml` too and found `DEC-016` (accepted 2026-09-04, same day as this mandate, `status: accepted`, `touches: []`) — it retires `agent-access` entirely (removes `crates/snapdown-bridge`, its Settings UI, its tests; supersedes `DEC-002`) and its own text says the removal "is a separate `/to-spec` → `/to-tickets` → `/implement-spec` pass, which this decision only unblocks." `crates/snapdown-bridge` still exists in the tree — confirmed by listing it, not assumed from the DEC- text alone. `FR-19`/`FR-20`/`FR-21`/`FR-22` (agent-access) are therefore not "closed FRs" in the sense the routing table means; they are a promise a decision has already withdrawn, and the withdrawal itself is unbuilt work | Declaring `every FR in scope closed` and moving to § Finish on the strength of `requirements-capture-to-markdown.yaml` alone | If wrong (the removal turns out already done somewhere this run hasn't looked), the next iteration's check catches it before any further work is dispatched on the strength of this row | — (no edit this iteration; the next firing applies `DEC-016` and dispatches its removal) |
| Iter 14 | wdi-autopilot Door 2 | Investigated before dispatching: grepped for a Local API server and an Access Key Settings UI anywhere in `apps/desktop` — neither exists (`LC-017`'s own registry comment already said so; the Slint rewrite apparently never carried the Access Key UI forward either). So `DEC-016`'s removal is narrower than its own Cost section feared: the standalone `crates/snapdown-bridge` crate, its workspace-member line, and `desktop-ci.yml`'s one-executable guard, which names `snapdown-bridge.exe` by hand. Dispatched the removal via `/to-spec`→`/to-tickets`→`/implement-spec` in a fresh worktree (`chore/remove-mcp-bridge`), scoped explicitly to code only — told the builder NOT to touch the corpus (`.what/`, `.how/`, `components.yaml`, `DEC-016`'s own file), since applying the decision to the corpus is a separate `wdi-decision` intent `apply` pass this coordinator owns | Dispatching a wide removal without checking what actually needs removing first, and letting the builder also touch the corpus in the same pass (self-review of its own decision-application) | If the investigation missed something, `cargo build --workspace`/`cargo test --workspace` inside the dispatch will surface it immediately | agent `add1f03b3181d2162`, no files changed yet — pending its report |
| Iter 14 | wdi-autopilot Door 2 | Split `DEC-016`'s landing into two separate passes — code removal (this dispatch) and corpus application (`wdi-decision` intent `apply`, retiring the `agent-access` Product Component across its SRS/SDD, the spine's Binds lists, the PRD, `components.yaml` and the two structure maps) — left for the next iteration rather than attempted in the same one | Doing both at once, which risks the corpus edit being made before the code change is confirmed safe, or a builder self-reviewing its own corpus edit | The two are independently reversible; landing them separately keeps each dispatch's scope legible, the same reasoning used for `BUG-103` vs `FR-34`/`FR-35` | — (no edit; logged as what a following iteration does) |
| Iter 15 | wdi-autopilot Door 2 | With the code-removal dispatch still running in its own worktree (no file overlap), started `DEC-016`'s corpus half in parallel rather than waiting: dispatched a second agent to apply the decision per `wdi-decision` intent `apply`'s own process — list every target first, dispatch each to its owning skill, quote the decision rather than paraphrase it, `.what/` before `.how/`, fill `touches:` last. Named every target the decision's own Cost section and the ownership table imply (SRS/SDD-agent-access, the spine's five `AD-N` Binds lists, `c4-l2-containers.md`, `components.yaml`, the two structure maps, the PRD, `requirements-agent-handoff.yaml`'s FR-19..22, any agent-access UC, and inventory rows), in its own new worktree (`docs/apply-dec-016`), explicitly told not to touch code and not to mark `DEC-016` `applied` unless every target genuinely landed | Waiting for the code removal to finish first, or doing this corpus work myself directly in the coordinator's own worktree | If a target was missed, `DEC-016` stays `accepted` rather than falsely `applied` — the dispatch was told to leave it that way rather than guess completeness | agent `ac2ecb7b9296b1918`, no files changed yet — pending its report |
| Iter 18, PR #40 | wdi-autopilot Door 2 | Code-removal builder reported done: three tickets, three commits (`32f6ed3`/`ebc8ea7`/`7c47a4a`), a separately-dispatched review with zero blocking findings on either axis, PR opened. It found **more than the dispatch brief named** — a full compiled `AccessKey`/`AccessKeyStore` domain layer in `snapdown-core`/`snapdown-store` with no caller, and a shipping "Agent Bridge" Settings tab the brief's grep missed (spelled with a space) — and removed both as squarely part of the same decision, not scope creep. Independently re-ran `cargo test --workspace --no-fail-fast` myself in its worktree — green | Trusting the report without an independent re-run, and treating the two extra removals as out-of-scope since the brief didn't name them | If my re-run had disagreed this row would say so; it agreed | `chore/remove-mcp-bridge` branch, PR #40 (not merged by this run) |
| Iter 18 | wdi-autopilot Door 2 | The review pass also found two stale doc references outside the diff (`codebase-stack-guide.md:40` still lists the deleted crate; `inventory-readers.py:199-211` still opens a deleted file, degrades gracefully) and two registry rows that now need closing (`BUG-59`, `OQ-6`, both about the surfaces just deleted) — reported, not fixed, per that dispatch's own scope. Left all four for the still-running corpus-application agent rather than interrupting it mid-flight or fixing them here myself | Sending the running corpus agent a mid-flight message to add scope, which risked confusing work already in progress | If the corpus agent's own investigation doesn't independently find these, the next iteration adds them explicitly once it reports | — (no edit; logged as a follow-up for the same agent's report or the next iteration) |
| Iter 20, PR #41 | wdi-autopilot Door 2 | Corpus-application agent reported `DEC-016` retired across 37 files (SRS/SDD-agent-access deleted, spine's five `AD-N` Binds trimmed, PRD rewritten, `components.yaml`/structure maps/inventories updated, `OQ-6` closed), raised the decision to `status: applied` on its own branch, and updated the baseline for the fossil findings this creates. Pushed the branch and opened **PR #41** myself (the dispatch wasn't asked to) | Leaving the work unpushed with no PR, unlike every other dispatch this run | Consistency — every other closed piece of work this run has a PR the owner can find | `docs/apply-dec-016` branch, PR #41 |
| Iter 20 | wdi-autopilot Door 2 | Independently re-ran `validate.py` in that worktree before trusting the report and found what looked like a real gap: 4 "stale review" findings the reported baseline update didn't cover. Traced it before "fixing" anything: `autopilot/r3` is running a **newer, uncommitted-to-`main` `validate.py`** (2540 lines, from this run's own earlier `wdi-method update`) where document staleness is advisory-only; the branch this PR targets still has `main`'s **older** validator (1684 lines) where the same condition is a hard `V13` failure — a real, unrelated version-drift finding, not a defect in the agent's work. Started adding the 4 lines to the baseline anyway, then found `AGENTS.md`'s own pre-existing text (§ "korpus.yml cannot see that half at all") **explicitly forbidding exactly this**: those findings never appear on the shallow-clone CI runner, so the baseline "MUST hold the runner's visible set, not the local one." **Reverted the edit before committing it** — confirmed with `git status` that nothing landed | Committing the 4-line addition, which would have broken this PR's actual CI by making the baseline demand findings the runner can never produce | The revert was caught before any commit; had it landed, the next iteration's independent CI check would have caught the resulting mismatch | — (edit made and reverted locally; nothing pushed) |
| Iter 20 | wdi-autopilot Door 2 | Confirmed CORRECT (after the near-mistake above): replicated `korpus.yml`'s exact `sed`/`grep`/`sort`/`cmp` gate against the agent's original, unmodified baseline — exact match, CI would pass as the agent left it | Trusting the agent's "confirmed green" claim without replicating the actual gate, or trusting my own first "mismatch" reading without checking `AGENTS.md` first | — | — |
| Iter 20 | wdi-autopilot Door 2 | Left `autopilot/r3`'s own `DEC-016` file at `status: accepted` (not `applied`) — the application lives on a separate, unmerged branch/PR (#41), and marking two divergent copies of the same decision `applied` independently would be confusing | Syncing `autopilot/r3`'s copy to `applied` too, ahead of that PR actually merging | If PR #41 is later revised, `autopilot/r3`'s copy staying `accepted` is the honest state | — |
| Iter 20 | wdi-autopilot Door 2 | Noted for the owner rather than acted on: `autopilot/r3` carries a `wdi-method update` (0.6.1) never merged to `main`, so every fresh worktree this run's dispatches create off `main` runs an **older** `validate.py`/`AGENTS.md` than this coordinator branch. Not a defect in any PR — each PR's own CI uses `main`'s committed version consistently — but worth landing `main`'s method update separately at some point so the two stop diverging | Silently living with the drift, or trying to fix it mid-mandate (out of this run's scope — it's a method-package update, not an `FR`) | If ignored indefinitely, baseline-format reconciliation gets harder each time; naming it now is cheap | — (observation only) |
| Iter 20 | wdi-autopilot Door 2 | Left four small follow-ups the two `DEC-016` dispatches found but correctly didn't fix (out of each one's own scope): `risks.yaml`'s 3 `agent-access` rows (no ownership row in the method for this file — a gap to report, not guess at); `BUG-59` needs a "moot/retired" status this registry has no convention for yet; `.what/_product-brief/brief.md` still describes the withdrawn MCP/Access-Key vision (a G1-level rewrite, bigger than either dispatch's scope); `OQ-1` still cites "the Local API" in one historical clause | Fixing these now, which risks inventing conventions (a defect-status vocabulary, a risks.yaml ownership rule) rather than reporting the gap | Small and low-risk either way; naming them means they aren't lost | — (no edit; logged as open follow-ups) |
| Iter 21 — FINISH | wdi-autopilot Door 2, routing table row "Every FR in scope closed" | Confirmed with a final pass rather than assumed: `grep -c` on both requirements registries totals 43, none carry `status: open` anywhere `specs.yaml` still names a wave, and every `.scratch/` feature besides `bundle-library` (already closed this run) was already `done`/`resolved` before this mandate began. `validate.py` GREEN. Moved to § Finish: `validate.py --generate` re-run, `DEC-018` raised to `status: applied` with `touches:` naming every file this coordinator edited directly (ledger, bundle-library tickets, `FR-41`/`FR-33` wording, `SRS-bundle`/`SDD-bundle`/`SRS-finding`, `inventory-screen.md`, `components.yaml`, `specs.yaml`'s `W8`, `defects.yaml`'s `BUG-103`) — the four dispatched builders' own commits are traced through their PRs, not duplicated into this `touches:` list. Cron job `8d3b4c61` cancelled | Starting another candidate-row iteration on the strength of the estimate table alone, which still lists every FR as a "forward-looking candidate" regardless of build status (its own header says so) | If a genuinely open FR surfaces later, it reopens through a new mandate or a direct `wdi-report`/`wdi-build` pass — this mandate's own authority ends at `applied` | `DEC-018` file + its `decisions.yaml` row, `.control/generated/*` (final regenerate) |
