---
topic: Snapdown architecture spine and the G3 blueprint
artifact: .how/_platform/ARCHITECTURE-SPINE.md
updated: 2026-08-22T23:00
---

- (event) G3 blueprint written in one pass: spine, C4 L1/L2/L3, three inventories, cross-cutting, business rules, five SRS, five domain models, five SDD skeletons
- (decision) paradigm: hexagonal. Rust domain core with ports; every UI and every agent-facing surface is an adapter. One promise implemented once, three handoff paths as translations
- (decision) stack set by the owner mid-run: Tauri v2 + Rust; React + Vite + TypeScript for both front ends; Go with net/http and chi for web-api. Next.js and Express excluded by the owner
- (decision) nine AD. Trimmed from eleven: the UUIDv7 id convention and the UTC timestamp rule moved to cross-cutting.md, because cross-cutting is their described home and an AD would be a second version
- (decision) AD-1 exists because a Marker and its Note line are one thing. Everything else in this product is replaceable; that binding is the product
- (decision) AD-8 born specifically for : a Publication slug unrelated to every Library id, so one leaked URL is not a way to find the next
- (decision) AD-9 born because the pressure to render a Bundle slightly differently per surface is constant. A golden-file test across the three paths is the only thing that catches it
- (decision) four containers, all built: true. Embedded SQLite and the Vault folder are NOT containers — no runtime anyone deploys. web-ui IS one: a SPA runs in the reader's browser, which is its own process
- (decision) five Product Components: finding, bundle, settings, agent-access, sharing. Sliced by object, not by moment — so that a Note is born and edited inside one component and AD-1 never spans a seam
- (decision) ownership: finding owns Finding/Note/Marker, bundle owns Bundle/BundleItem, settings owns Setting, agent-access owns AccessKey, sharing owns Publication. platform_owns is empty on purpose
- (decision) mode: global outline. finding, agent-access, sharing raised to guarded; settings lowered to catalog, so its G4 is skipped by design
- (decision) risk_accepted low on four components, medium on settings. Nothing is high, so V23 needs no risk-acceptance DEC-
- (decision) 23 UC, four critical: UC-7 delete Findings, UC-12 delete a Bundle, UC-17 issue an Access Key, UC-20 publish. Every one is an irreversible action or a disclosure that cannot be recalled
- (decision) FR-4 image reduction carries no_uc with its reason: no actor, no initiating step. It is a property of UC-1 and UC-2, asserted by NFR-3
- (decision) 30 BR, all cross-component by construction. Local rules wait for G4
- (note) V25 reports four findings — no code map heading for the four built:true containers. Correct at G3: the registry holds the plan, the map holds the tree, and no folder may be listed before a file creates it. Closes at the first wave
- (note) two dangling citations in installed files are a METHOD defect, not ours: .control/structure-*.md templates cite .constitution/structure-guide.md, which lives at .constitution/method/structure-guide.md. Fixed in our re-derived maps; the templates in the package still carry it
- (note) the spine template's WDI override names .how/_platform/architecture/ as the home; architecture-guide.md repeals that folder and names .how/_platform/ARCHITECTURE-SPINE.md. The Accepted guide wins; the template disagrees and that is a method defect to report
- (event) spine excluded from doc_standards by design — reviewed manually through wdi-review before G3 closes
- (note) correction to the AD-8 entry above, which lost a word to shell quoting: AD-8 was born specifically for the sharing component. The log is append-only, so the entry stands and this is its fix
- (event) 2026-08-23 G3 REOPENED over the delta only, per Step 8. Not a re-run: the portrait is living and amended. Both intents, catalog then platform
- (change) catalog: glossary — Quality Budget rewritten against DEC-004 (it named the setting PAIR, which was the pre-DEC-004 shape); Advanced, Auto, Sharp, Balanced, Small, Custom, Persona, Orphan report added. 18 entries -> 26, re-sorted alphabetically
- (decision) Sharp, Balanced and Small got three entries rather than one shared entry. A combined entry reads tidier and is unfindable: a reader looking up "Balanced" scans alphabetically and does not find it under S
- (change) catalog: UC-24, UC-25, UC-26 added for CAP-9, all on `settings`, none `critical`
- (decision) none of the three is `critical`, and that is worth stating because they are the reason every other use case is reachable. The rule is money, personal data, or an irreversible action. Importance is not on that list, and stretching `critical` to mean `important` is how the count passes a third and stops meaning anything
- (change) catalog: settings domain model — Quality Budget is now ONE choice with two DERIVED companions, not three independent Settings. Under Auto the long edge and encoder quality are not stored at all; they are computed per Capture. Invariants 9 and 10 added
- (decision) that derivation is exactly why NFR-18 exists: with the numbers not in the Setting, the only place the truth can live is on the Finding that was produced. Found by reading DEC-004's Cost section, not by reading the requirement
- (change) catalog: BR-103..BR-109 added to business-rules.md. All seven are genuinely cross-component; BR-107 (one colour authority, both themes) and BR-109 (every primary surface reachable, including a frozen one) bind every component that draws
- (change) platform: AD-10 and AD-11 added. Nine invariants -> eleven
- (decision) AD-10 (colour has one authority) qualifies as an invariant rather than a convention on the strict test: breaking it in ONE component breaks ANOTHER. `finding` and `bundle` paint light panels; the shell paints theme-following text. Neither is wrong alone. The defect exists only where they meet, and it shipped
- (decision) AD-11 (one process owns the Library) passes the same test through the stores: finding-store, bundle-store and settings-store have no cross-process lock discipline and no test covering one, because the shape has always been single-writer. A second executable would introduce a second writer silently — nothing fails at build time, and the first corruption appears under concurrency nobody reproduced
- (event) DEC-003 was weighed against Snagit's two-process shape and rejected it. The C4 L2 container count is UNCHANGED, and that is now recorded as the outcome of a decision rather than the absence of one. A later reader would otherwise find no trace that the alternative was considered
- (change) platform: C4 L2 and L3 AMENDED, never regenerated. L2 gained the two personas on the desktop-app row plus the amendment note; L3 gained three corrected `mode` values, the window frame on the `settings` row, the UC-24/UC-25 relationship, and a note on why the personas are not two boxes
- (change) platform: cross-cutting gained two agreements — "Colour, theme, and contrast" and "State a control does not yet know"
- (decision) the second one is an agreement rather than one control's detail because the Windows startup registration is the only OS-owned control TODAY. The next one will be written by someone who did not read FR-18
- (change) platform: inventory-screen gained row 0, the Editor shell, numbered 0 rather than 16
- (decision) numbers here are stable and a new row normally takes the next one. The shell is not a new surface — it is the frame every other row has always been drawn inside, and it was missing rather than added. Numbering it 16 would place the frame after the things it contains. Recorded in the file as the one exception, explicitly not a precedent
- (event) inventory findings: rows 2, 7 and 9 had carried NO build unit for five waves. wdi-ux born LC-029, LC-030, LC-031 for them; LC-028 for the shell, which had neither row nor build unit. A row with no LC is a promise nobody checks
- (event) `derived_from: plan` still holds for all three inventories. .constitution/project/inventory-readers.py has never been written, so the plan-versus-code difference has never been computed. That absence is what let three unbuilt rows sit unnoticed, and it is recorded as the finding rather than papered over
- (change) structure-codebase.md re-derived. Its stamp read commit 90d55a7 "after merging Wave W1 stories" — four waves stale — and two `built: true` containers, mcp-bridge and web-api, had no heading at all. V25 was refusing both
- (event) validate.py --generate run. 12 findings -> 6. Three V3 (UC-24..26 unscheduled) resolve at G5, one V22 (settings g4_passed) resolves at G4, two V24 are citations inside a BMad package file and are not this product's to fix
- (note) NOT done: wdi-review has not run on the spine or any SRS. The rule is that G3 MUST NOT open on a portrait that has not been through it. Recording it as done would be the one failure the check list exists to prevent
