---
topic: Snapdown — bundle component depth
artifact: .how/bundle/SDD-bundle.md
updated: 2026-08-31T21:11
---

- (event) 2026-08-23 G4 re-run at `deep`, raised from an inherited `outline`. g4_passed reset for the same reason as `finding`
- (change) behaviour: 03-domain/state-machines.md and SCN-05. design: Inherited Constraints and Failure Behaviour written for the FIRST time — at `outline` the SDD stopped after section Structure — plus ABCE, contracts, data-model, and LC-013
- (event) **BUG-1 FOUND.** bundle_item.finding_id carries ON DELETE CASCADE to finding(id), and finding_store.rs:29 sets PRAGMA foreign_keys = ON. Deleting a Finding DELETES ITS BUNDLE_ITEM ROWS. FR-13's third consequence has been `active` since G2 and says the opposite: "A Finding that belongs to a Bundle can still be deleted; the Bundle keeps its own copy of the image and stays readable"
- (event) the damage is quiet, which is what makes it bad. bundle.markdown is a column, so AD-9 holds: the document still reads correctly and still copies the same bytes. Only the ITEM LIST loses a row. The delivered document and the record of the delivered document disagree, and nothing reports it
- (decision) registered as BUG-1, a DEFECT and not planned work. The requirement predates the schema and was never withdrawn — this is code disagreeing with something already `active`, which is exactly the line defects.yaml exists to hold. Planned work is where a requirement is NEW
- (event) why it went undetected for five waves: finding_store.rs:362 asserts the cascade to `note` and `marker`, which is correct. NOTHING asserts the ABSENCE of a cascade to bundle_item. A test that a cascade does NOT fire is one nobody writes unless a document says the cascade must not exist — and that document did not exist until this gate. This is the clearest argument in the whole pass for why raising `mode` was worth the cost
- (decision) the fix is probably to drop the FOREIGN KEY entirely, not only its cascade. A BundleItem legitimately refers to a Finding THAT MAY NO LONGER EXIST, which is the precise condition a foreign key exists to forbid. Rows already lost are not recoverable
- (decision) state-machines section 2 draws the BundleItem's relationship to its source, and drawing it is what surfaced the bug. `Orphaned` here means something DIFFERENT from `finding`'s `Orphaned`: there it is a fault to be repaired, here it is the normal, correct end state of a Bundle that outlived the Findings that fed it. The name collision is recorded rather than renamed away
- (decision) MarkdownWriter being PURE is the load-bearing choice behind AD-9. One function, called once, output stored in a column — three code paths cannot drift because there is nothing to keep in step. Storing both bundle.markdown and bundle.markdown_path is duplication on purpose: the column is the truth for every in-product path, the file is what a Reviewer or an agent opens from the Vault
- (note) [PARTIAL] filed: whether a composition failing partway leaves image copies in the Vault was not verified
- (note) NOT done: wdi-review has not run
- (change) G3 delta 2026-08-31, wdi-blueprint intent catalog. BR-11 narrowed from 'A Bundle is never edited in place' to 'changed only by the composer writing it again over the Bundle's own copy'; AD-9 left INTACT because all its clauses govern the way out and its closing clause names changing the composer as the remedy. BR-10, BR-59 and BR-65 verified still true and explicitly marked not-to-be-tidied. New BR-122 for the derived sealed state. UC-28 (FR-39 export PDF) and UC-29 (FR-40 edit title/notes) added, both critical: false with the derivation stated. Actor Register lost 'choose in the same act whether its source Findings go too' - FR-14's withdrawn consequence. domain-model State Lifecycle and invariant 4 rewritten, invariant 10 added. Glossary Bundle entry no longer says 'recomposed rather than edited'; Sealed added.
- (event) NOT DONE and reported instead: SRS-bundle's Decision Summary still claims 'the clipboard, the Local API, and a published page all serve identical bytes - three handoff paths, one document'. FR-12's amendment of 2026-08-31 permits the clipboard to render absolute image links, so that sentence is now false for one of the three. Fixing it means settling whether FR-12 contradicts AD-9, and this skill MUST NOT edit an AD-N or decide one - wdi-decision owes that DEC- first. BR-11's new wording was deliberately written WITHOUT restating AD-9's byte-identity clause so that this question is not frozen by accident.
- (change) G4 behaviour 2026-08-31, applying DEC-012 into SRS-bundle.md. Six sections corrected, every one as a stated correction quoting the old wording: Decision Summary (the 'clipboard, Local API and published page all serve identical bytes - three handoff paths' clause, false twice - identity is not the promise, and two of the three paths have no code; also the stale 'runs at the global mode: outline' when components.yaml made it deep on 2026-08-23), Constraints (the 'those exact bytes' row split into three rows citing AD-9/DEC-012, BR-11/DEC-012, BR-10), Non-Goals (three withdrawn entries recorded, plus a NEW narrower non-goal - the Finding SET is still fixed, which is the half of the old prohibition that was doing the work), Success Signal ('yields that file unchanged' -> 'that same document'), Assumptions (OQ-12 struck through, OQ-1 marked still-untested because FR-12 has no implementation), Risks (the claim that a golden-file test covers three paths - there is one test, it pins the stored form, and it covered one path even when written).
- (change) G4 design 2026-08-31, narrow correction pass on SDD-bundle.md for DEC-012. Site 1 (Inherited Constraints): AD-9's retired title replaced and its new Rule now quoted VERBATIM as a blockquote, matching AD-2's style in the same section - it had never been quoted, only paraphrased. Added the new obligation this places on bundle: it runs the composer, so AD-9's rebase permission is exercised here or nowhere. Site 2 (LC-011 purity): purity claim verified against markdown.rs:25 and kept; the claim that it makes 'AD-9's golden-file test possible across all three handoff paths' retired - one test, pinning the composer against a reference, and two of the three paths have no code. Logged [MISSING]: a rendering-parity test across paths never existed and cannot exist until a second path does. Site 3 (composed Markdown is a column): checked and SURVIVES, with one line saying why, because its neighbours did not. Site 4 (binds: [AD-1, AD-2, AD-9, AD-10]): confirmed still correct, unchanged.
- (event) REPORTED, NOT WRITTEN: SDD-bundle.md's realizes: is [UC-9, UC-10, UC-11, UC-12] and does not list UC-28 (export PDF) or UC-29 (edit title/notes), which were born today. Deliberately left, because realizes: would claim this design covers them and it does not - there is no exporter design, no LC for one, and ticket 07 defers the exporter's packaging decision to the Export PDF effort so an LC MUST NOT be invented. FR-39, FR-40 and NFR-19 need a REAL design pass at mode: deep, not a citation fix. Same for the SDD's Decision Summary and Failure Behaviour: FR-39 adds a boundary (a file written outside the Vault) that the Failure Behaviour section does not cover.
- (change) BR-5 WIDENED and BR-12 NARROWED 2026-08-31, closing two wdi-review findings. BR-5: the owner was given two options for a save that lands in the database but fails on bundle.md, and chose one rule rather than two - a partly saved Bundle is not a state the product may leave behind. Widened rather than joined by a sibling because the invariant was always all-or-nothing on disk and deletion was merely the only direction that existed when it was written; FR-40 added as a source. The clause that an unsaved edit survives is load-bearing: without it all-or-nothing means the Reviewer loses their typing to a full disk. Two rows added to SDD-bundle Failure Behaviour for it and for Export PDF, the only boundary in this component writing outside the Vault; the Export PDF row names NO LC because ticket 07 defers the exporter's packaging.
- (event) A wdi-review finding turned out VOID, and the code is what voided it. It claimed discarding one Bundle's originals would silently seal another Bundle sharing a Finding. Unreachable: main.rs:269-283 collects every Finding id any Bundle holds and filters those out of the filmstrip - its own comment says 'anything a Bundle already holds leaves it' - and the filmstrip is the only place assembly selects from. So BR-12's old wording, 'One Finding may belong to several Bundles', was false as a promise about the product and had been for as long as the filmstrip existed. Narrowed to what is true, which is true of the STORE: bundle_item is unique on (bundle_id, finding_id), not on finding_id, so the shape is legal data that no surface reaches. BR-122 cites BR-12 and was checked - it stays true, because it reads the sealed state from whether the Findings exist rather than from how many Bundles held them.
