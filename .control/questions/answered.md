# Answered

**Loaded when:** looking for a decision that was reached through a question, not through a `DEC-`.

Rows move here from the other three lists. MUST NOT be deleted.

## Answered

| id | Question | Answer | Closed |
|---|---|---|---|
| OQ-22 | Serving a published Bundle as raw Markdown in a bare `<pre>` is sufficient, because Snapdown's reader is a machine and the human reader those inventory rows promise is not actually wanted | **Yes, sufficient for now.** `DEC-020`, closing `BUG-2`: the publish dialog (row 11, `LC-022`) and the reader's not-found page (row 15) are withdrawn outright — neither was ever built and `DEC-005` forbids building either now. Row 14 is corrected rather than withdrawn, because `DEC-015` had already found `LC-027` genuinely serves the route, just as a bare `<pre>` dump rather than the designed `PublishedBundleReader.tsx`. Not a permanent renunciation: if `DEC-005` lifts and the owner wants the three screens built, the promise can be reopened through `wdi-product` | 2026-09-04 |
| OQ-6 | The reviewer prefers pasting a key per session over the agent holding standing access to the library | **Moot — closed by `DEC-016`, not answered.** The owner instructed on 2026-09-04 that the Agent bridge/MCP feature be removed outright: the copy-Markdown-and-paste workflow already covers what it was for, so there is no more key ceremony to have a preference about. The friction this row asked whether the Reviewer felt no longer has anything to be friction against | 2026-09-04 |
| OQ-29 | Seven behaviours the owner asked for on 2026-08-28 had no `FR-` under any capability: canvas **zoom**, user **resize** of a stored image, **paste** from the clipboard, **copy** a burned image, a **context menu**, **crop**, and **undo/redo** | **Four became requirements, one already was one, two are refused.** `FR-34` zoom, `FR-35` paste, `FR-36` copy burned image and `FR-37` context menu are now in `requirements.yaml` and in PRD §4.9. `undo`/`redo` needed nothing: `FR-33`'s consequences already say *"Redo/Undo history is supported for canvas additions, moves, edits, and deletions"* - the assumption was written from a defect row that had misread it, and the code now implements it. `crop` and destructive `resize` are REFUSED: both are named non-goals in `SRS-finding.md` and in the Product Brief, and the refusal is load-bearing rather than tidy - `AD-9` promises byte identity and the Vault keeps no second copy. Reversing that is a `DEC-`, not a story. The Crop toolbar button stays inert on purpose | 2026-08-28 |
| OQ-12 | Recomposing a bundle is acceptable in place of editing its written Markdown | **No — the assumption turned out false, and `FR-40` is what replaced it.** A Reviewer can now correct a composed Bundle's title, its Bundle notes, and the note text on the Findings inside it, saving as one act; recomposing is no longer what anyone does to fix a typo. Three things settled it, in order: the owner asked for it through the Bundle Library map, so `wdi-product` intent `update` landed `FR-40`; `BR-11` was narrowed the same day from *"A Bundle is never edited in place"* to *"changed only by the composer writing it again over the Bundle's own copy"*, with the old wording preserved in `.what/business-rules.md` § Amended; and `DEC-012` recorded the reading of `AD-9` that makes both legal — settled by `AD-9`'s own **Prevents**, not by a new argument. **The cost this row named did not materialise, and why is worth keeping:** it feared Bundles drifting from the Library that produced them. That drift is still guarded — `BR-10` keeps a Finding's edits out of a Bundle already holding it, and `FR-40` writes only the Bundle's own stored copy and never reads a Finding. The assumption's mistake was concluding that forbidding *all* editing was the only way to prevent it; the narrower rule prevents it by construction | 2026-08-31 |
| OQ-34 | Does `AD-9` reach a PDF export, or is a PDF outside it because its reader is a person? | **No - `AD-9` does not reach a PDF export, and the assumption is confirmed.** Owner's answer, 2026-09-01, over two alternatives: narrowing `AD-9` a second time so a PDF becomes legal under it, or letting `AD-9` reach PDF unnarrowed - which would have ended `FR-39` in the shape ticket 07 researched. What settles it is `AD-9`'s own **Prevents**, which names the harm as *two agents* reading one review and disagreeing about it; a PDF is a human artifact and `Copy Markdown` is the agent path, so the named harm cannot arise through it. This is the same move `DEC-012` made on 2026-08-31 - letting Prevents decide where the Rule over-claims - and the consistency is the argument, not a convenience. **The boundary MUST be written into `AD-9` itself**, which was the condition attached to the answer: a reader of the Rule alone (*no re-rendering*) would otherwise conclude the opposite, which is exactly how this gap stayed invisible until the spine's missing `CAP-12` row was filled. **Routed to `wdi-decision`.** Stating an `AD-N`'s scope is expensive to reverse and identical in kind to `DEC-012`, so it lands as a `DEC-` and the spine is amended from it by `wdi-blueprint` intent `platform` - never by an in-place edit to an `AD-N`. `FR-39` is unblocked once that `DEC-` is applied, and not before | 2026-09-01 |
| OQ-20 | Are Snagit and Cobalt Capture the right experience benchmark for a product whose reader is a machine, not a person? | **The benchmark is split rather than kept whole or discarded.** Owner's answer, 2026-09-01, option B of three. The **capture** side - the half a person operates with their hands - keeps Snagit and Cobalt Capture as its benchmark. The **handoff** side is measured against `BG-2` and `BG-3` instead, time and tokens, because that is the half whose reader is a machine and where a human-tool benchmark spends effort on affordances an agent cannot use. The row's feared cost was real and is now bounded rather than denied. What the split buys is **checkability**: *median handoff under 120 seconds* can be verified, *feels as good as Snagit* cannot, and `DEC-005` lifts only on a bar that is **met and verified** by its own wording. **Feeds ticket 06** (`.scratch/bundle-library/issues/06-write-and-verify-the-g2-experience-bar.md`), which runs `wdi-ux` to write the bar; this row settles the bar's *shape*, not its content, and ticket 06 still owes the content | 2026-09-01 |
| OQ-31 | Is `BG-2` a close enough goal for `CAP-12` (Export PDF) that a new business goal is not needed? | **No - `BG-2` is not close enough, and a sixth business goal is owed.** Owner's answer, 2026-09-01, option C, over amending `BG-2`'s measure or leaving the misfit merely recorded. Two mismatches, and the second decided it. `BG-2`'s measure - *median handoff time for a five-Finding review under 120 seconds* - cannot report on an exporter at all. And `BG-2` promises *no file management* while Export PDF exists precisely to produce a file a person manages, so `CAP-12` was serving a goal whose own words it contradicts. Stretching that phrase to admit an exporter would weaken a promise the product is holding elsewhere; a different audience - someone without Snapdown - earns its own goal. **Routed to `wdi-problem`:** birthing a `BG-` is a G1 act and MUST NOT be taken from G2. Until it exists, `CAP-12`'s `goal:` stays at `BG-2` with the comment already in `requirements.yaml` naming the misfit, and that comment MUST NOT be deleted before the new goal lands | 2026-09-01 |
| OQ-33 | Is choosing a different Vault folder an irreversible action, so that `settings` at `risk_accepted: high` needed a reason other than the one written? | **Yes - it is an irreversible action, and `settings`' `risk_note` understated what was accepted.** Owner's answer, 2026-09-01. `FR-16` plus `BR-29` (*either moves every existing file or moves none*) moves every stored file on disk. All-or-nothing guarantees **atomicity, not reversibility**, and the note conflated the two: a move that completes is not undone by choosing the old folder back when that folder sat on a drive since removed, a share since dropped, or a disk since reformatted. **`DEC-013` is unaffected and stands.** `settings` was covered either way; only the reason recorded beside it was wrong, and correcting it costs one sentence. **Routed to `wdi-init` intent `risk`** for the note, which also clears a second staleness in the same comment block: it still calls `DEC-013` *still draft* when that decision is `applied`. The note MUST NOT be reworded to slip past V23 - that check matches keywords against `risk_note` as prose deliberately, so a note admitting an irreversible action trips it, and tripping it is the check working rather than failing | 2026-09-01 |
| OQ-27 | Is `web/ui` (`@snapdown/ui`) left in place, untouched, now that nothing in the active workspace builds or tests it? | **Deleted - not archived, and not kept.** Owner's answer, 2026-09-01, choosing removal over the `archive/` move that was recommended to them. Verified before the answer: **zero consumers of `@snapdown/ui` anywhere in `apps/` or `crates/`** - `DEC-007` moved the desktop app to Slint and `apps/web-service` (Go) never consumed it. What is given up is a head start if `sharing`'s published-Bundle reader is ever rebuilt as a web frontend (`BundleComposer.tsx`, `MarkerLayer.tsx`, `ConfirmDialog.tsx`, `AgentAccessView.tsx` and the rest); the owner judged that speculative, and `OQ-22` separately doubts that surface needs more than a bare `<pre>`. What is bought is the end of a directory every sweep, audit and new agent has to learn is dead - the same cost the stale `npm --prefix apps/desktop` lines exacted for weeks after `DEC-007`. Git keeps the history, so any component is recoverable by path at any commit. **This row records the decision; the removal is code work and is NOT done by `wdi-question`** - it also strips the `web-check` CI job of its only subject and voids the `web/ui` block in `AGENTS.md` section Verification, so it lands as one considered change rather than an `rm` | 2026-09-01 |
| OQ-23 | Is a composition test class - one asserting a component is MOUNTED and reachable, not merely correct - worth adding as a standing convention? | **Yes - a standing convention, and answering it exposed a repair that could not wait.** Owner's answer, 2026-09-01, option B. The convention: every UI `LC` carries one test asserting it is **instantiated and its callbacks are wired**, not merely that it behaves correctly in isolation. It answers the most expensive failure this repository has had - four components built, unit-tested and mounted nowhere, found in a single sweep on 2026-08-23 (`CaptureOverlay`/`BUG-4`, `MarkerLayer`/`BUG-5`, `OrphanReportView`/`BUG-6`, and `EmptyState`), leaving `FR-1`/`FR-2`, `FR-8` and `FR-15` unmet across four waves in a build whose tests all passed. `V12` cannot catch it: it checks that an `LC` is **registered**, not that it is **reached**. **The repair:** `AGENTS.md`'s standing workaround says to grep for `<ComponentName` across `apps/desktop/src` and `web/ui/src`. That is JSX syntax; `apps/desktop/src` has been Rust since `DEC-007`, and `web/ui` is being deleted under `OQ-27` - so the only guard standing against this defect class points at no live code at all. **Not routed to a `DEC-`:** a test convention is not expensive to reverse. It belongs in `.constitution/project/codebase-conventions-guide.md`, with `AGENTS.md`'s grep line corrected for Slint in the same act. Extending `V12` was rejected as the wrong address - `validate.py` lives in `.constitution/method/`, which is replaced in full on every update | 2026-09-01 |
| OQ-25 | Is `V11`'s rule - every PAIR of stories in a wave must share a `touches` value or sit on a `depends_on` path - the right shape for a wave of unrelated defect fixes? | **Void - the question can no longer bite.** Closed by the agent on 2026-09-01 rather than by the owner, and deliberately: it fails all three of `blocking.md`'s tests and is settled by fact rather than by judgement, which is the case this list's own rule says the agent MUST take itself. `V11` runs only over stories in `waves.yaml`, and the wave layer is retired - `AGENTS.md` keeps that file as history and forbids extending it. No wave will ever be validated by `V11` again, so whether its pairwise `touches`/`depends_on` rule suits a wave of unrelated defect fixes is a question about a check that cannot fire. It joins `V3`, `V12` and `V19` as a fossil. **What the row observed stays true and is why it is archived rather than struck:** satisfying `V11` on W7 forced a `depends_on` chain encoding intended *order* while the field means *build-blocking*, so the registry states a dependency that does not exist. W7 carries an honest comment saying so, and that comment is now the surviving record of it | 2026-09-01 |

A row arrives here by moving from `blocking.md`, `assumptions.md`, or `external.md`, keeping its
id. First arrival: `OQ-29`, 2026-08-28.

`OQ-12` arrived 2026-08-31 from `assumptions.md`. It went here rather than into that file's own
§ Answered table, which holds `OQ-21`: that row was kept beside the open assumptions because its answer
was a decision **not** to act and the reasoning had nowhere else to live. `OQ-12`'s answer is a decision
to act, and its reasoning is already held by `DEC-012`, `prd.md` § 4.4, `SRS-bundle.md` § Assumptions,
and `business-rules.md` § Amended — so the archive is the right home and no reasoning is lost by it.

**A note on `OQ-29` above, added 2026-08-31.** Its answer refuses `crop` and destructive `resize` partly
on the grounds that *"`AD-9` promises byte identity and the Vault keeps no second copy"*. `DEC-012`
narrowed `AD-9` on 2026-08-31, and a reader who checks `AD-9` now will not find the phrase. **The
refusal is unaffected and stands.** The byte identity it relies on is between *a Bundle's image copy and
the Finding's image*, which `DEC-012` does not touch — that decision is about the **document's image
links** on a handoff path. This note exists because the row is an archive that MUST NOT be rewritten,
and because the refusal it records is load-bearing.

**Seven rows arrived together on 2026-09-01**, the largest single closure this file has taken, and the shape of
that batch is worth recording. Six were the owner's, answered in one pass after being put to them with the
background, the analysis and the options for each - `OQ-34`, `OQ-20`, `OQ-31`, `OQ-33`, `OQ-27`, `OQ-23`. The
seventh, `OQ-25`, was closed by the agent as void: it fails all three of `blocking.md`'s tests and its answer is
a fact about a retired validator, not a judgement anyone owns.

**Four of the six leave work behind, and none of that work is done by closing the row.** `OQ-34` and `OQ-31` are
routed onward - to `wdi-decision` and to `wdi-problem` respectively - and until those land, `FR-39` MUST NOT be
specced and `CAP-12` keeps serving a goal it is known to misfit. `OQ-33` owes a `risk_note` correction through
`wdi-init` intent `risk`. `OQ-27` owes a deletion that is code work, and `OQ-23` owes a convention plus a repair
to `AGENTS.md`'s composition-check guidance, which currently names a directory that is being deleted and a
syntax the desktop stopped using at `DEC-007`. **An answered question here is a decision recorded, never a
change made** - that distinction is what this file is for.

## Corrections to the 2026-09-01 batch, made while working its debts

The six answers above were acted on the same day, and doing the work found three things wrong with how
they were recorded. The rows themselves MUST NOT be rewritten - they are the archive - so the
corrections live here.

**`OQ-31`'s answer says "a sixth business goal is owed". The new goal is `BG-8`.** `BG-6` (*the tool
never becomes the thing being managed*) and `BG-7` (*Snapdown's own surface costs the Reviewer no
attention*) have both existed since G1, so the phrase was already wrong in the open row it was copied
from, written 2026-08-31. `BG-8` - *a review is readable by someone who does not have Snapdown* - was
born in `requirements.yaml` and in the brief on 2026-09-01, and `CAP-12`'s `goal:` moved from `BG-2` to
it. Ids are allocated from the highest ever used; nothing about the answer changes.

**`OQ-27`'s answer names "the `web-check` CI job". No job has ever had that name.** The job was
`shared-ui-check` in `.github/workflows/desktop-ci.yml`, and it was one of four rather than one of
three. The error was copied from `AGENTS.md`, which had carried it uncorrected; both are fixed now, and
the job is removed along with the package it tested.

**`OQ-23`'s answer reads as though the convention had to be invented. It did not - a working example
was already in the tree.** `apps/desktop/tests/test_annotation_wiring.rs`, built for `CAP-11` under
`BUG-72`, opens with *"`CAP-11` is REACHABLE, not merely built"* and asserts exactly what the
convention now requires. That mattered practically: `codebase-conventions-guide.md` forbids being
filled before code exists that ratifies it, so the existence of that file is what allowed the
convention to land there and the guide to rise from `Draft` to `Accepted` with `ratified_by: a06a8f3`,
rather than sitting as an aspiration in `AGENTS.md`.

**One thing the debts also settled that no row had asked.** `OQ-20`'s split lands on `BG-7`'s own
wording in the brief, not only on ticket 06: `BG-7` was where the sentence *"the bar is the experience
of Snagit and of Cobalt Capture"* actually lived, applied to the whole product. The capture half keeps
that benchmark; the handoff half is measured against `BG-2` and `BG-3`.
