---
topic: Wave W8 - capture becomes real. Three simulations replaced by real code (BUG-14), the Note at capture time built (BUG-18), the test surface converted from measuring dimensions to measuring images, and the burn wired to its caller (BUG-19).
updated: 2026-08-24T09:40
---

- (direction) Caller (wdi-build, wave W8) dispatched: project waves.yaml W8 onto a SPEC. Five stories, chain S1..S5, test names carried verbatim.
- (decision) The crate goes at crates/snapdown-capture, because ARCHITECTURE-SPINE.md:245 already put it there and assigned CAP-1 and CAP-2 to it. This is not a new design decision; it is building one that was made at G3 and never executed.
- (constraint) The dimension arithmetic already written is CORRECT and must be kept: compute_reduced_dimensions_for_pair, W6-S4's Auto resolution, and the fixed presets. This wave replaces the bytes between them, not the arithmetic.
- (constraint) AD-4 governs the burn: an image is reduced exactly once, at capture, and no original is kept. burn_markers therefore draws on the ALREADY-REDUCED bytes and MUST NOT re-reduce them.
- (constraint) SCN-04 asymmetry binds what is drawn: a Marker with no Note line is reported in the note pane and NEVER annotated on the image. An app-only state must not be burned into an artifact read on another machine under another theme.
- (question) BUG-18 found while surveying: CaptureOverlay never asks for a Note. FR-2 unmet, UC-1 half-built, LC-029 unimplemented. Registered and scheduled as W8-S4 rather than left as an inventory UNREAD.
- (constraint) MUTATION IS THE ACCEPTANCE CRITERION for the test conversion, not a nicety. W7 proved why: a test that read correctly and asserted a plausible thing still passed with its own defect reinstated.
- (assumption) Two third-party dependencies are unavoidable - a screen capture crate and an image codec. Neither capability can responsibly be written from scratch. The specific crates are W8-S1's decision to make and record.
- (direction) Caller re-derived the SPEC in place after waves.yaml gained a sixth story, W8-S6, and defects.yaml gained BUG-19. Same slug, same folder, so CAP-1..CAP-3 and the five existing stories.yaml entries are unchanged. The three W8-S6 test names are carried verbatim from waves.yaml, inside invoke_dev_with - stories-schema.md has no tests field and says structure goes inside the string.
- (decision) W8-S6 is projected as CAP-4, matching the spine's own "CAP-4 Bundles" row rather than inventing a wave-local id. The work is bundle composition, and the spine already governs it with AD-1, AD-2, AD-3 and AD-9.
- (constraint) W8-S6 introduces NO behaviour. SDD-bundle.md:36 already says Markers are burned at compose time into a copy, data-model.md:63 already says image_path is the Bundle's own copy, LC-010 already owns "burn the images" as one transaction, and LC-012 already owns the normalised-to-pixel conversion. The promise exists; the call does not.
- (constraint) AD-2 binds the unit of work: the bundle_item row must not be committed before its file exists. UC-9's failure flows are the acceptance shape - a failed burn abandons the whole composition (BR-5), a store write failing after the files are written removes them again, and BR-13 refuses the composition outright when a selected Finding's image is missing.
- (constraint) A Finding with no Markers still gets its own image copy, per UC-9 alternate flow 1. W8-S3's "burning no markers returns the source bytes unchanged" is what makes that copy byte-identical - it is not licence to skip the write.
- (question) The .webp/PNG disagreement is NOT resolved here. OQ-26 is cited in the SPEC's Open Questions and in the story's invoke_dev_with: the corpus names no image format anywhere, and OQ-26 frames both branches (encoder choice recorded as a DEC-, or a promise belonging in the spine as an AD-N). This SPEC decides neither.
- (assumption) No corpus gap was found for W8-S6. Everything the story needs is already written in .what/bundle/ and .how/bundle/, which is why it is a defect fix rather than a design story. Nothing was invented and nothing in .what/ or .how/ was edited.
