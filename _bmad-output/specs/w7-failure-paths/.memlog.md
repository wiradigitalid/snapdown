---
topic: Wave W7 — three defects that share one habit: a failure the code declines to report (BUG-12 panic on unopenable store, BUG-3 unescaped Markdown in the published page, BUG-10 empty error message to an agent)
updated: 2026-08-24T06:30
---

- (direction) Caller (wdi-build, wave W7) dispatched headless: project waves.yaml W7 onto a SPEC; three defect-fix stories, test names carried verbatim; no new behaviour.
- (constraint) DEC-005 freezes sharing and agent-access. It permits this wave by its own sentence: 'This decision does not forbid a fix. It forbids new work.' No new FR, no use case, no UX pass, no depth above guarded.
- (decision) Spec folder is _bmad-output/specs/w7-failure-paths/ (no 'spec-' prefix), following the six existing folders w1..w6, not run_folder_pattern's 'spec-{slug}'. The repo convention is established and consistent.
- (decision) One SPEC for the whole wave across three components, per wdi-build Phase 2 ('One wave = one SPEC ... spec_folder: one per wave, not one per wave x component'). This CONTRADICTS this skill's persistent_fact 'A spec slug MUST take the shape w<N>-<pc>, one run per (wave x Product Component) pair'. wdi-build is the method skill and wins; W6 already resolved it the same way. Reported as a method defect.
- (capability) CAP-6 (settings) is the capability W7-S1 restores: the tool keeps out of the way, which presupposes it starts at all. Allocated from requirements.yaml, not invented.
- (capability) CAP-8 (sharing) is what W7-S2 restores; CAP-7 (agent-access) is what W7-S3 restores. Both allocated from requirements.yaml.
- (constraint) BR-118 is not in question and MUST NOT be weakened to make the startup report easier: the store is opened, never created over, and a store that cannot be read is reported WITH ITS PATH.
- (constraint) SDD-settings.md Failure Behaviour, row LC-025 -> library.db, already specifies W7-S1's required behaviour verbatim. The story implements an existing spec; it writes no new one.
- (question) GAP REPORTED UPSTREAM: no NFR covers output encoding on the published page. BUG-3 records contradicts:[NFR-15], but NFR-15 is about enumeration and identical refusals, not about rendering a Note as text rather than markup. The six sharing NFRs (NFR-10..15) were read; none fits. The fix is unambiguous regardless; the missing promise is a corpus gap for wdi-product, and MUST NOT be invented here.
- (assumption) AD-7 + BR-17 are the true anchor for W7-S3 and the fit is exact: BR-17 says a refusal is always distinguishable from an empty result, and an error whose message is the empty string is not.
