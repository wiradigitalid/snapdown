---
topic: Wave W7 — three defects that share one habit: a failure the code declines to report (BUG-12 panic on unopenable store, BUG-3 unescaped Markdown in the published page, BUG-10 empty error message to an agent)
updated: 2026-08-24T07:02
---

- (direction) Caller (wdi-build, wave W7) dispatched headless: project waves.yaml W7 onto a SPEC; three defect-fix stories, test names carried verbatim; no new behaviour.
- (constraint) DEC-005 freezes sharing and agent-access. It permits this wave by its own sentence: 'This decision does not forbid a fix. It forbids new work.' No new FR, no use case, no UX pass, no depth above guarded.
- (decision) Spec folder is _bmad-output/specs/w7-failure-paths/ (no 'spec-' prefix), following the six existing folders w1..w6, not run_folder_pattern's 'spec-{slug}'. The repo convention is established and consistent.
- (decision) One SPEC for the whole wave across three components, per wdi-build Phase 2 ('One wave = one SPEC ... spec_folder: one per wave, not one per wave x component'). This CONTRADICTS this skill's persistent_fact 'A spec slug MUST take the shape w<N>-<pc>, one run per (wave x Product Component) pair'. wdi-build is the method skill and wins; W6 already resolved it the same way. Reported as a method defect.
- (capability) CAP-6 (settings) is the capability W7-S1 restores: the tool keeps out of the way, which presupposes it starts at all. Allocated from requirements.yaml, not invented.
- (capability) CAP-8 (sharing) is what W7-S2 restores; CAP-7 (agent-access) is what W7-S3 restores. Both allocated from requirements.yaml.
- (constraint) BR-118 is not in question and MUST NOT be weakened to make the startup report easier: the store is opened, never created over, and a store that cannot be read is reported WITH ITS PATH.
- (constraint) SDD-settings.md Failure Behaviour, row LC-025 -> library.db, already specifies W7-S1's required behaviour verbatim. The story implements an existing spec; it writes no new one.
- (question) GAP REPORTED UPSTREAM: no NFR covers output encoding on the published page. BUG-3 records contradicts:[NFR-15], but NFR-15 is about enumeration and identical refusals, not about rendering a Note as text rather than markup. The six sharing NFRs (NFR-10..15) were read; none fits. The fix is unambiguous regardless; the missing promise is a corpus gap for wdi-product, and MUST NOT be invented here.
- (assumption) AD-7 + BR-17 are the true anchor for W7-S3 and the fit is exact: BR-17 says a refusal is always distinguishable from an empty result, and an error whose message is the empty string is not.
- (event) SPEC review ran (structure + prose + edge-case-hunter), dispatched read-only claude worker: 3 high, 10 medium, 9 low. Report at _bmad-output/specs/w7-failure-paths/review-W7-spec.md. Every high and every medium upheld.
- (decision) H1 UPHELD and verified independently: BUG-12 was already fixed by W6-S5 (aa30434). init_app_stores, StartupError, format_startup_error_message, show_native_message_dialog and the fallible setup hook are all at HEAD; the five .expect() store opens are gone. The register row was stale and a planner was dispatched against it. BUG-12 -> resolved with the full history recorded.
- (decision) H3 UPHELD and verified independently across all five stores: journal_mode=WAL runs BEFORE quick_check, so a valid-header/corrupt-pages database IS written to and -wal/-shm ARE created beside it. BR-118 is broken by the code path that reports it. Registered as BUG-15, high. The existing byte-identity test cannot reach it because its only fixture is garbage bytes, which SQLite rejects before any pragma.
- (decision) H2 UPHELD: the .expect() at lib.rs:347 is now the ROUTINE exit path for a store failure, so BUG-12's exclusion of it ('nothing left to report with') has expired. Registered as BUG-16, medium. Second time an exclusion outlived its reason.
- (decision) M9 UPHELD: MessageBoxW with a NULL owner and no MB_SETFOREGROUND/MB_TOPMOST can open behind other windows - the exact symptom BUG-12 existed to end. Registered as BUG-17, medium. Whether it is visible stays a MANUAL check; asserting the flags were passed would test a copy of the input.
- (decision) W7-S1 RE-SCOPED rather than cancelled: it now carries BUG-15, BUG-16 and BUG-17. Its old story file is discarded and Step 1 re-run, because its entire intent-contract was written against work already done.
- (decision) M8 UPHELD: cross-cutting.md says 'The MCP Bridge does not invent its own errors.' W7-S3's planned free-form strings carry no catalogue code. The remedy must synthesise a catalogue code (internal), and AD-7's own rule - a refusal is distinguishable BY ITS CODE - is what makes that the point rather than a nicety.
- (decision) M6 UPHELD: W7-S2's slug fixture 'test<slug>&42' cannot exist on Windows (illegal filename chars) and store.go joins the slug into a path. CI runs Go on ubuntu, so it would be green while the developer machine is red. Fixture changed to one hostile to HTML but legal as a path segment.
- (decision) M1, M2, M3 UPHELD and carried into SPEC.md: the DEC-005 vehicle justification now appears in the SPEC and not only in waves.yaml; the Open Question now distinguishes a missing PROMISE from a fix that NEEDS a promise, which is DEC-005's reversal trigger; the Why/Open-Questions contradiction about BUG-3 is resolved.
- (question) M7 UPHELD as a diagnosis, REJECTED as a remedy. The depends_on chain is indeed invented - the three stories share no file - but depends_on: [] on all three makes V11 fail on all three pairs. The chain stays, is documented in waves.yaml as intended order rather than a build gate, and the underlying tension is filed as OQ-25.
- (event) Findings resolved into SPEC.md, waves.yaml, three new defect rows (BUG-15/16/17), BUG-12 -> resolved, and OQ-25. spec_reviewed trace written on wave W7 at sha 7c67dfb.
