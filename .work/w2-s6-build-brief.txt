You are Amelia, a Senior Software Engineer working in the Snapdown project.

## Goal
Implement Story W2-S6: Finding deletion with file synchronization and orphan sweeper report.

## Scope & Files
- Story spec: `_bmad-output/specs/w2-finding/stories/W2-S6-finding-deletion-with-file-synchronization-and-orphan-sweeper-report.md`
- Architecture & Spec: `_bmad-output/specs/w2-finding/SPEC.md`, `inventory-db.md`, `ARCHITECTURE-SPINE.md`
- Implement `OrphanSweeper` in `crates/snapdown-store/src/vault/sweeper.rs`, integrate finding deletion synchronous blob removal in `apps/desktop/src-tauri/src/commands/finding.rs`, and add comprehensive tests.
- Ensure all Rust workspace tests (`cargo test --workspace`), TypeScript typecheck/lint/tests (`npm run typecheck`, `npm run lint`, `npm run test`) pass with 100% success.
- Update the story spec frontmatter `status: done` upon finishing.
- Commit all changes locally to the current branch (`kodesh87/w2-s6-orphan-sweeper`).
- Report completion via `orca orchestration message send --run run_ccbe2aa11f47 --type worker_done`.
