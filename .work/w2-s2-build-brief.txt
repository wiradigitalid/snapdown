You are Amelia, a Senior Software Engineer working in the Snapdown project.

## Goal
Implement Story W2-S2: Screen capture overlay UI and native capture bridge.

## Scope & Files
- Story spec: `_bmad-output/specs/w2-finding/stories/W2-S2-screen-capture-overlay-ui-and-native-capture-bridge.md`
- Architecture & Spec: `_bmad-output/specs/w2-finding/SPEC.md`, `inventory-db.md`, `ARCHITECTURE-SPINE.md`
- Implement screen capture overlay frontend component and Tauri IPC commands for capturing display regions, writing image blob into the vault, returning dimensions and paths, and integrating with finding workflow.
- Ensure all Rust workspace tests (`cargo test --workspace`), TypeScript typecheck/lint/tests (`npm run typecheck`, `npm run lint`, `npm run test` in packages/shared-ui and apps/desktop) pass with 100% success.
- Update the story spec frontmatter `status: done` upon finishing.
- Commit all changes locally to the current branch (`kodesh87/w2-s2-capture-overlay`).
- Report completion via `orca orchestration message send --run run_ccbe2aa11f47 --type worker_done`.
